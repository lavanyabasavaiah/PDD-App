class AppConfig {
  // http://10.0.2.2:5000  → Android Studio Emulator (maps to host localhost)
  // http://localhost:5000  → iOS Simulator
  // http://<LAN-IP>:5000   → Physical device (phone must be on same WiFi as PC)
  static const String backendUrl = 'https://1dkllnz4-5000.inc1.devtunnels.ms/api';
}
